<%  unless @categories.blank? %>
	$('.category_result ul').append("<%=escape_javascript(render :partial => @categories)%>");
	$('.scroll_down').attr('data-page','<%= @categories.next_page%>')
	<% else %>
	$('.scroll_down a').hide();
	<% end %>

	   